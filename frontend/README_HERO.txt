OAM Hero — device / download version
====================================
This replaces ONLY src/Hero.tsx.

From your frontend/ folder:
  cd /Users/mac/Desktop/oam-platform/frontend
  unzip -o oam_hero_devices.zip

Then the dev server hot-reloads. New hero:
  • Left: green field, drifting icons, headline, red + outline CTAs SIDE BY SIDE
  • Right (desktop): two phones (Android + iOS) running an OAM app mock,
    a download caption, and Google Play + App Store badges
  • Mobile: phones stack below the text automatically

Notes:
  • Store badges link to "#" — the app isn't published yet (placeholders).
  • Phones are pure CSS/SVG — no image files needed.
